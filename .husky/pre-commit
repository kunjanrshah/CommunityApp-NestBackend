#!/usr/bin/env bash

. "$(dirname "$0")/_/husky.sh"

# lint-staged runs ESLint --fix and Prettier --write only on staged files.
# --no-install prevents npx from downloading anything at commit time.
npx --no-install lint-staged
