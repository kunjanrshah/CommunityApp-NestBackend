#!/usr/bin/env bash

. "$(dirname "$0")/_/husky.sh"

# Run Commitizen only when /dev/tty is available (interactive terminal).
# Skip gracefully on CI, GUI commit tools, or platforms without /dev/tty.
if [ -e /dev/tty ]; then
  npx cz --hook < /dev/tty || true
fi
